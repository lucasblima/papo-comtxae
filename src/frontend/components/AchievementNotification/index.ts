/**
 * DEPRECATED: This file re-exports from the canonical location
 * Please import AchievementNotification from '../ui/AchievementNotification' instead
 * Or even better, use the barrel file: import { AchievementNotification } from '../ui';
 */

// Re-export the named export
export { AchievementNotification } from '../ui/AchievementNotification';

// Re-export the default export
export { default } from '../ui/AchievementNotification';

// Re-export types if needed
export * from '../ui/AchievementNotification/types'; 